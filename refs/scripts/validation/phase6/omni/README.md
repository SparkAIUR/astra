# Phase 6 Omni Assets

This directory is reserved for Omni-specific manifests, request payloads, and optional
Talos simulation assets used by `phase6-omni-integration.sh`.

Current run path uses synthetic API-level provisioning pressure when a runnable Omni image
is not supplied via `OMNI_IMAGE`.
